---
title: "BinarySearch"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

#LINEAR SEARCH

search = function(array, given, equal) {
  if (abs(v[mid] - t) <= eps) {
      return(mid)
    }
    else if (v[mid] < t) { # C format ok in a function
      lo = mid + 1
    }
    else {
      hi = mid - 1
    }
    return(0)
  
}
                
```

